# Copy to .env and fill in your Postgres connection string.
# For Vercel deploys, set this in Project → Settings → Environment Variables instead.
DATABASE_URL=postgresql://user:password@host:5432/app_db
